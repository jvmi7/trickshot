node_modules
dist
.DS_Store

# Rust / Tauri
src-tauri/target

# Per-platform Tauri ACL schema generated by `cargo build` on Linux (the repo
# tracks the macOS/desktop schemas the maintainer builds; this one is a local
# build artifact).
src-tauri/gen/schemas/linux-schema.json

# Compiled sidecar binaries (built via `bun run build:sidecar`)
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep
